# PHASE 13H MANIFEST

## Created

- `docs/native-cms-layout-plan.md`
- `PHASE13H_REPORT.md`
- `PHASE13H_MANIFEST.md` (this file)

## Modified

- `src/app/_types/storefront.ts`
- `src/app/_components/Footer/FooterComponent/index.tsx`

## Deleted

None.

## Validation

Not run in this environment (no network access, no `node_modules`
available to install). See "Validation Results" in `PHASE13H_REPORT.md`
for what was manually verified in place of running the compiler, and
run these before relying on the change:

```
npm run lint
npx tsc --noEmit
npm run test
```

## Zip contents

This phase's zip contains only the five files listed above (created +
modified), at their full repository-relative paths, so they can be
dropped directly into the existing tree.
